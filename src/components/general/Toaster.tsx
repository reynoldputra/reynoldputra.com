"use client"
import { ToastContainer } from "react-toastify";

const Toaster = () => {
  return (
    <ToastContainer
      style={{
        width: "360px",
      }}
    />
  );
};

export default Toaster;
